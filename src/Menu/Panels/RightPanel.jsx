import { Servers } from '../Componants/RightPanel/Servers';

export const RightPanel = () => {
    return (
            <div className="rightPanel">
                <Servers />
            </div>
    );
}